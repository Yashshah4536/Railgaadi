# RailGaadi Environment Variables
# Copy to .env.local and fill in values.
# Server-only keys (never exposed to the browser):
RAILRADAR_API_KEY=
OPENWEATHER_API_KEY=
OPENTOPOGRAPHY_API_KEY=

# Public key (necessarily public — restrict by origin in MapTiler dashboard):
NEXT_PUBLIC_MAPTILER_KEY=
